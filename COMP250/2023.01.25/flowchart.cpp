#include <iostream>
using namespace std;

int main(){
    int a{}, b{}, k{};
    // some other code
    if (k < 90){
        if ( k < 80) {
            lower += 1;
        } else {
            b++; // b = b + 1;
        }
    } else {
        a++; // a += 1;
    }
}